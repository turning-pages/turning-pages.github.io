source "https://rubygems.org"

# Matches what github.com uses when building GitHub Pages. Requires a modern
# Ruby (3.x recommended). Run: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
